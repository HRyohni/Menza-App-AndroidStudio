<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.menzaappp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/donelogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/donelogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MenzaAppp">
        <activity android:name=".About"></activity>
        <activity android:name=".custom_list_viewv2" />
        <activity android:name=".FastFood" />
        <activity android:name=".CustomListView" />
        <activity android:name=".MainActivity"/>
            <activity android:name=".SplashScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>